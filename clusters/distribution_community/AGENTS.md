# Distribution Community Instructions

- Read `CONTEXT.md` completely before changing this cluster.
- Measure observable reachability, not market validation or audience size.
- Keep conversation, search, audience-mediated, mixed, and passive-only
  reachability distinct.
- Prefer repeated recent activity, independent authors, exact problem language,
  and explicit access paths over size metrics.
- Keep discovery snippets separate from hydrated surface evidence.
- Keep future logic in `distribution_community.py` after shared interfaces exist.
