# Cluster-Specific Instructions

Replace this introduction with the cluster name and its most important operating
constraints.

- Read `CONTEXT.md` completely before changing code, evidence, or profile output.
- Use supplied PDFs and long-form text to model the cluster Markdown files; do
  not retain those inputs unless requested.
- Treat `CONTEXT.md` as canonical for cluster scope and terminology.
- Preserve detailed user guidance unless the user explicitly supersedes it.
- Rename `cluster_name.py` to `<cluster_id>.py`; the Python filename must match
  the cluster directory.
- Keep cluster-specific logic in that module.
- Maintain all nine evidence sections in `PROFILE.md`.
- Link every factual profile item to raw evidence identifiers.
- Record durable context changes in the `CONTEXT.md` change log.
