# Cluster-Specific Instructions

- Read `CONTEXT.md` completely before changing this cluster.
- Keep cluster-specific logic in `<cluster_id>.py` after shared interfaces exist.
- Link shared contracts and provider references; do not copy them here.
- Preserve raw evidence IDs through every derived output.
- Do not create a cluster-local nine-part profile.
