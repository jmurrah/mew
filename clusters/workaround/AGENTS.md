# Workaround Instructions

- Read `CONTEXT.md` completely before changing this cluster.
- Require job/problem, concrete substitute behavior, and friction/cost for a
  qualifying workaround.
- Keep ordinary flexible-tool use and `adjacent_signal` separate from confirmed
  workaround evidence.
- Preserve exact behavior, cadence, artifacts, consequences, and raw IDs.
- Keep future logic in `workaround.py` after shared interfaces exist.
